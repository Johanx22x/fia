circuito (palabra[inicio] == palabra[fin]) {
  box (inicio >= fin) {
    confirmacion verdadero;
  } out {
    box (bebez == bebe) {
        confirmacion verdadero;
    }
  }
  
  inicio = inicio + 1;
  fin = fin - 1;
}
